# ADR 004 — Render.com

Status: proposto.

Publicar Client, Server e PostgreSQL separadamente. Não usar o filesystem local para mídias permanentes. Configurar CORS, health checks, segredos e migrations controladas.
